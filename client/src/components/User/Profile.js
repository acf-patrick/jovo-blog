import Loading from "../Loading/Loading";

const UserProfile = () => {
  return (
    <Loading message="Loading user profile...">
      <h1>Hello</h1>
    </Loading>
  );
};

export default UserProfile;
